# Changelog

## 0.0.1
- Initial package with agent core types, registry, runtimes, and built-in agents.

## 0.0.2
- Fix stdin handling to avoid hanging agents.
- Make agent creation async and surface initialize errors.
- Add Aider agent and expand OpenCode/Claude Code capabilities.
- Add resilience utilities for rate-limit detection and fallback management.
- Add Docker runtime helpers for container and network discovery.
